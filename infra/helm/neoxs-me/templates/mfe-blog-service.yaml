apiVersion: v1
kind: Service
metadata:
  name: mfe-blog
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "neoxs-me.labels" . | nindent 4 }}
    app.kubernetes.io/component: mfe-blog
spec:
  type: ClusterIP
  selector:
    {{- include "neoxs-me.selectorLabels" (dict "name" "mfe-blog" "Release" .Release) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.mfeBlog.port }}
      targetPort: {{ .Values.mfeBlog.port }}
      protocol: TCP
